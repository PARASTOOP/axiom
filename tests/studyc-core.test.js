const assert=require('assert');const C=require('../js/studyc-core.js');function ready(){const s=C.createSession();C.addExample(s,'A',{pulseCount:1,gapLength:1,beamWidth:1});C.addExample(s,'A',{pulseCount:2,gapLength:1,beamWidth:1});C.addExample(s,'B',{pulseCount:5,gapLength:5,beamWidth:5});C.addExample(s,'B',{pulseCount:4,gapLength:5,beamWidth:5});return s}(function(){assert.strictEqual(C.distance({pulseCount:2,gapLength:3,beamWidth:4},{pulseCount:2,gapLength:3,beamWidth:4}),0,'T01');let s=ready(),p=C.predict(s,{pulseCount:1,gapLength:1,beamWidth:2});assert.ok(p.ok,'T02a');assert.strictEqual(p.labelId,'A','T02b');let b=s.examples.find(e=>e.labelId==='B');C.editExample(s,b.id,{features:{pulseCount:1,gapLength:1,beamWidth:2}});p=C.predict(s,{pulseCount:1,gapLength:1,beamWidth:2});assert.strictEqual(p.labelId,'B','T03');C.addExample(s,'B',{pulseCount:5,gapLength:4,beamWidth:5});C.editExample(s,b.id,{labelId:'A'});p=C.predict(s,{pulseCount:1,gapLength:1,beamWidth:2});assert.ok(p.ok,'T04a');assert.strictEqual(p.labelId,'A','T04b');s=C.createSession();C.addExample(s,'A',{pulseCount:1,gapLength:1,beamWidth:1});C.addExample(s,'A',{pulseCount:1,gapLength:1,beamWidth:2});C.addExample(s,'B',{pulseCount:1,gapLength:1,beamWidth:1});C.addExample(s,'B',{pulseCount:5,gapLength:5,beamWidth:5});p=C.predict(s,{pulseCount:1,gapLength:1,beamWidth:1});assert.strictEqual(p.kind,'UNSURE_TIE','T05');assert.strictEqual(p.nearest.length,2,'T07a');assert.ok(p.nearest.every(x=>x.diffs&&Number.isFinite(x.diffs.pulseCount)&&Number.isFinite(x.diffs.gapLength)&&Number.isFinite(x.diffs.beamWidth)),'T07b');s=C.createSession();C.addExample(s,'A',{pulseCount:1,gapLength:1,beamWidth:1});assert.strictEqual(C.predict(s,{pulseCount:1,gapLength:1,beamWidth:1}).ok,false,'T06');s=ready();p=C.predict(s,{pulseCount:1,gapLength:1,beamWidth:1});const a=C.makeAttempt(s,p,'INITIAL'),snap=JSON.stringify(a.datasetSnapshot);C.editExample(s,s.examples[0].id,{features:{pulseCount:3,gapLength:3,beamWidth:3}});assert.strictEqual(JSON.stringify(a.datasetSnapshot),snap,'T08');assert.strictEqual(C.transition(s,C.STATES.REFLECT).ok,false,'T09');const r=C.restore(C.serialise(s));assert.deepStrictEqual(r.examples,s.examples,'T10');r.settings.reducedMotion=true;assert.strictEqual(r.labels[0].mapping.shape,'circle','T11');assert.throws(()=>C.logEvent(r,'CONFIDENT_USER',{}),'T12a');C.logEvent(r,'WHY_OPEN',{});assert.strictEqual(r.events.at(-1).eventType,'WHY_OPEN','T12b');console.log('Study C core tests: PASS')})();